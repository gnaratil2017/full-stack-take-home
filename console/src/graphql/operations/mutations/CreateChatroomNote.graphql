mutation CreateChatroomNote(
  $chatroomId: ID!
  $content: String!
) {
  createChatroomNote(
    input: {
      chatroomId: $chatroomId
      content: $content
    }
  ) {
    chatroomNote {
      ...ChatroomNoteData
    }
  }
}
